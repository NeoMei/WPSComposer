# Windows final native acceptance — stage 4

**Windows acceptance: PASS.** This is the Windows gate; overall release/merge remains with the parent candidate task. Source includes d024bd4 (Mac add-in only); Windows COM source hashes are unchanged from the native validation runs. See `final-validation.json` and `provenance.json`.

- Word and WPS three schemes: six native cases, two chapters each with H1–H4, insertion/deletion/reopen/PDF: **30 checks pass**. Chinese-formal and hybrid exactly follow the shared contract; hybrid displays 第一章 / 1.1 / 1.1.1 / 关键工法01： and second-chapter 2.1 / 2.1.1. Word uses ListLevel.LinkedStyle to retain one hierarchy; WPS retains its supported style-side binding.
- Auto: read-only alternate registry views find the real WPS 32-bit LocalServer32 registration from 64-bit Python. Public generate/convert choose WPS, confirmed by artifact metadata; source DOCX bytes are preserved. No registry or global settings were changed.
- Fresh public representative: **PASS**, 382.343 seconds, actual generate(DOCX), convert_to_pdf, generate(PDF), overwrite refusal and source preservation. DOCX passes shared numId, styles, section policy and content checks. Both PDFs show empty cover footer, TOC i, body 1/2/3. Eleven independent PDF/XML checks and all five rendered pages pass review.
- Structural representative: **12 native checks pass**. Insert H1 changes the original H1–H4 to 2 / 2.1 / 2.1.1 / 2.1.1.1; deleting it restores numbering. Moving the chapter from physical page 3/logical 1 to physical page 4/logical 2 updates the three-level TOC, survives save/reopen, and matches the six-page PDF (footer sequence empty/i/1/2/3/4). Original public DOCX is unchanged.
- Actual Word UI: Ctrl+S, close, Ctrl+F12/open the exact saved path, and navigate to the moved chapter. The synchronous observer verifies the same owned PID 30192, all six heading numbers/pages, TOC, text hash and unchanged saved file bytes on reopen (**5 checks**). Final UI close leaves zero documents; verified app Quit follows. Cropped UI evidence excludes account toolbar. Earlier edit/Undo/save/close/reopen evidence remains in round1; media/REF and user-sentinel evidence remain in round3.
- Cleanup: **28 recorded task Word PIDs absent**, including the safely cancelled helper host. Preexisting Word PID 9252 is still present. Retained old probes and the earlier uncertain timeout host are listed separately in `final-postflight-round4.json`; no claim is made that all historical probes exited.
- Complete Windows coverage: full suite **2838 passed, 45 skipped, 1 deselected**, 702.99 s, 20 existing Pillow deprecation warnings; the sole file-list isolation test was then run serially and **passed** in 4.68 s. It was separated because concurrent native evidence creates files. No unresolved Windows test failures. After the Mac-only merge, **24 executor tests + 1 sealed-manifest check passed**; unchanged COM native cases were not repeated.

## Retained diagnostic failures

The original monolithic `native-edit-03/report.json` remains FAIL because its asynchronous UI observer raced a save/close modal after all 12 structural checks passed. It is not relabeled. `ui-reopen-verification.json` independently completes the UI gate with serialized observation and zero-document Quit; `final-validation.json` combines these completed gates. `native-edit-02` was cancelled after Word Find repeatedly returned a TOC-field match; body-scoped Find fixed only the helper. Its exact-path/PID cancellation and final PID absence are recorded. `auto-01` used unavailable psutil; auto-02 switched to existing WMI and passes. Earlier failed binding/probe artifacts remain unchanged.

Unit RED/GREEN remains archived: schemes 4 failed/4 passed before fix then affected suite 902 passed/10 skipped; registry 3 failed/2 passed before fix then routing/detection 27 passed. These are supporting checks, not substitutes for native acceptance.
