# Windows Word 原型本地独立复核

2026-09-08，控制端在 macOS 的独立工作区复核 Windows 证据分支。
验收范围是 Word/PDF 原生能力原型，未接入生产 API、合并主分支或安装发布。

- 证据基线：`a5c0813`；原始执行脚本字节补证：`233b240`。
- 核对全部发布证据校验和、每轮产物校验和，以及 run08 未保存哨兵与
  原生 runner 的非空前后快照：一致。哨兵明确不保存关闭，已确认的任务
  进程退出，注册实例保持运行。
- 独立读取 run05、run06、run08 的 DOCX XML 和 PDF：82 行、3 列、
  全部 80 条记录、原生标题编号关联、12pt 仿宋样例正文及双字符缩进、
  TOC/PAGEREF、重复表头和末尾标记均通过。每轮均 9 页。
- 使用 pdfplumber/PDFium 独立渲染，三轮全部 9 页像素完全一致；目视检查
  run08 的第 1、2、9 页。保留原型的版式限制：目录标题在条目之后，
  表格未设边框，允许行跨页，末页较空；不宣称成品模板验收。
- 原始 `windows_word.py` 含 209 个 CRLF 和 62 个 LF 换行；字节快照
  匹配原运行 SHA256，规范化 CRLF 后与对应 Git blob 完全相同。

独立审查发现：在文档窗口 PID 与候选 Word PID 相等之前设置
`exclusive_instance=True`，会让 Add/Name/Hwnd/PID 查询异常路径退出
尚未确认归属的应用。修复将退出权限与 Visible 设置一并移到 PID 核验
成功之后；仍仅关闭本次创建且持有引用的测试文档。

新增 6 项主流程回归：修复前 5 失败、1 通过；修复后全部通过。
独立复审确认该问题已解决。5 项既有身份判断测试通过。
修复后完整平台无关测试：**2630 passed, 12 skipped，175.21 秒**。
跳过的原生平台测试不计作此次 Windows 实测。

最终清理补丁尚待 Windows 对精确提交复验；以前的成功产物不被改写。
首轮失败遗留的 PID 9252 启动归属未完全证明，应用未被强制关闭；该限制
仍保留在原始报告中。生产接入仍待独立实现与完整验收。
