# Bounded private Excel launcher acceptance

Native run01 executes the reviewed bounded launcher through public `open_document(engine="msoffice")`: existing populated worksheet deletion, write, XLSX save, native PDF export, strict close/quit, and separate private read-only reopen all pass. The PDF parses as five pages. Both exact Excel births disappear; both persistent helper PIDs exit with code 0 after RELEASE. The original two unsaved synthetic workbook markers/alerts and source bytes are preserved. Five production hashes and the runner hash remain unchanged.

The parent has no AppKit launch FFI on the default path. Fake-child tests cover blocking discovery/launch/probe, partial IPC, parent EOF during FFI, lost replies, absolute shared deadlines and helper cleanup. These fault tests do not launch real Office; the native normal run does not inject a hung Office process. Unknown launch outcomes remain quarantined without Office signals or automatic retry.

The retained test-isolation incident records an earlier RED test accidentally launching one pristine private Excel process before any task AppleEvent. Parent exact-identity/bootstrap cleanup and birth disappearance are recorded separately, with original sentinels preserved. The global test guard now rejects default live Cocoa/launcher/OSA helper paths. Historical evidence is unchanged.

This is scoped native lifecycle acceptance. Word/PowerPoint and Windows native parity, complete capability certification, final installed-package verification and release approval remain separate gates. The previous run04 UI/PDF appearance acceptance remains tied to that earlier lifecycle source; current worksheet XML equality is recorded in comparison.json.
