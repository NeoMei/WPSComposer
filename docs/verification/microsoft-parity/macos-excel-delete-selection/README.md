# Excel logical selection after worksheet deletion

The old session retained a positional worksheet index after deleting a preceding or selected fresh empty sheet. A subsequent `write_cell` could target a different worksheet or fail out of bounds. The repair updates that index only after native deletion succeeds: surviving selected sheets keep their identity after indices shift, and deleting the selected sheet chooses its predecessor. Existing-sheet deletion remains unsupported; no deletion permission or global Excel setting changes.

The four new regression cases first produced two failures against the old implementation. `red-reproduction.py` and `red.log` independently retain that result against the exact `5622a16` module loaded in a test process; the working source is not replaced. The repaired session plus edit preflight pass 137 tests. Independent review finds no scoped issue, with 94 Excel script/session tests and 196 logical-index combinations passing; reviewed hashes are in `review.json`.

`run-01/report.json` records real Excel acceptance for deleting before, at, and after the logical selection. Each case creates its own two empty sheets in an isolated copy, deletes one, writes through the direct API, saves, closes and reopens. All three markers occur only on the expected surviving sheet. The parent separately parses saved OOXML and verifies the same name-to-cell mappings in `run-01/parent-verification.json`. All recorded source hashes still match. The original input file is unchanged, the nonempty unsaved synthetic sentinel is preserved and closed, and the two pre-existing unsaved workbooks remain in the original inventory. Inventory equality checks names, paths and saved flags, not every cell of pre-existing workbooks.

The separate UI copy passes actual A1 editing to `UI-DELETE-CHECK`, Undo, explicit Save, close and exact-path reopen. The reopened `DeleteProbeB!A1` contains `AFTER DELETE before-selected`. The parent then closes only that owned UI file and verifies the original workbook inventory. `ui-verification.json` binds the copy and hashes; the original native output is unchanged. UI Save may change package metadata, so byte identity of the UI copy is not required.

Fixture review then identified two coverage/cleanup weaknesses: exact sheet identity was not asserted by the fixture, and a lost creation acknowledgement could leave the owned sentinel unidentified. The parent and reviewer independently verified run01's saved files had the correct exact sheet sequences, so its observed success remains valid. The repaired fixture asserts the complete sequence after deletion and reopen. Its recovery requires exactly one added workbook, the random marker and unsaved state, and checks the marker/state again inside close. Uncertain or foreign identity remains a failed run requiring manual reconciliation, never an automatic close. Two portable fault-injection cases cover matching and foreign marker outcomes. Independent re-review passes 96 related tests with no remaining scoped finding.

`run-02` passes all three native cases with that strengthened fixture, the same production hash, saved source preservation, sentinel preservation and restored inventory. The parent again parses saved OOXML to verify exact ordered sheet names and marker locations. These are measured facts about the owned documents; no unmeasured content preservation of unrelated workbooks is inferred.

Full39 passes **5,044 tests / 12 skips** from snapshot `ed0da48783daaa47eb9389bfd722b86fd5f2d0ae`, with 385 unchanged source/configuration hashes matching live at that checkpoint. The fixture and its two new fault-injection tests were subsequently repaired and have a separate final regression record in `../full-round40`. This closes the scoped positional-selection defect. Complete Microsoft/WPS parity, existing-sheet deletion, Windows native acceptance and the remaining Word capabilities stay open.
