# Task audit after round36

This audit starts at runtime candidate `0b4f86e35baf09e58c3b817cde4e41a8b991f597` and now includes reviewed table-guard candidate `237691e1e016a3b1fc75febb5b383867f0d4a435`: full37 passes 5,040 tests / 12 skips with 384 unchanged source/configuration hashes; hosted CI07 passes all four Linux/Windows jobs (5,008/44 each Linux; 4,977/75 each Windows). The approved baseline remains 628 required rows across two platforms/1,256 gates. All six tasks remain partial. No missing requirement is removed to manufacture completion.

## Newly closed scoped defects

- Windows primary error/cancellation preservation across recovery metadata, quarantine, logs, child termination and lock closure; original failures and independent reviews retained.
- Early bridge POST rejection response truncation for origin/authentication and invalid/oversize Content-Length, with bounded teardown and unchanged authorization.
- Windows session budget rounding above600, proven by hosted raw request and real Python worker reproduction; clamp retains the strict limit and original deadline.
- Local full36:5,001 passed /12 skipped;383 source/configuration hashes unchanged. Hosted CI06 now passes all four Linux/Windows Python 3.9/3.12 jobs (4,969/44 each Linux; 4,938/75 each Windows), and does not establish installed Office native/UI operation.

## Still incomplete

- Mac Word still declares71/80 direct names;9 missing. Four declared anchored-quality methods have incomplete native acceptance because full snapshots can dirty document state or time out. Drawings/equations, strict middle insertion and general local rollback remain requirements.
- Native semantic-table construction: make-at-document appends; make-at-range inserts after a paragraph; explicit range conversion returns1×3 instead of4×3. Grid conversion failed after prefilling text. Each run is separately retained with exact owned/sentinel cleanup. No current constructor qualifies for public semantic-table support.
- Public Mac nonterminal structural table insertion can misplace content. An interim whole-request preflight rejection is implemented. Independent review found that patches-as-operations could bypass the before-open guard; the narrow combined-batch fix passes independent re-review (320 tests, including both unchanged reviewer reproductions). This prevents known wrong edits but does not fulfill the baseline.
- Argument/format/active-session/save-copy and all platform-specific acceptance rows remain to be certified on the final candidate.
- Windows Remote Control latest read returns the old interrupted task, not fresh candidate commands. Windows Office/WPS native edit/Undo/save/reopen and installation remain unverified for this candidate.
- Office.js supplement design and official API feasibility are prepared. A concrete read-only Word capability probe now passes independent re-review (21 Node / 6 Python tests; 19 frozen files), but requires ReadWriteDocument permission for its Word-specific reads. It is retained in officejs-word-probe-prepared; native enablement remains unanswered. The package does not implement snapshots, a command bridge or parity operations. No add-in, certificate trust or security change has been made. WPS ET add-in trust remains a separate unanswered gate.

Original requested outcomes—complete all tasks, no remaining worthwhile bugs, and comprehensive final backend/frontend/native/UI acceptance—are not yet met. Runtime testing, native diagnostics, scoped code review, draft PR, release and installed-client acceptance remain distinct claims. PR9 stays draft; v0.9.0 remains the latest released version.
