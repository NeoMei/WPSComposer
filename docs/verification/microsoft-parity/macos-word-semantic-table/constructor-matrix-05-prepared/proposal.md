# Designed 4×3 range-to-grid constructor proposal — offline only

One bounded experiment on one new private copy of the unchanged seed. This is a designed constructor: one grid-prepopulation write followed by one explicit text-range conversion. It is not another make-location variant. No native execution, public forward or production modification has occurred.

1. Retain and verify the original owned body, prefix, suffix, old table and exact sentinel preimages. Activate the exact owned window and bind the main-story selected 12:19 REPLACE range using `create range boundDoc start 12 end 19`.
2. Replace those seven characters with the exact ASCII grid below. Every cell is nonempty and unique; rows use CR, fields use tab, with no final CR in the grid. The original marker paragraph's terminal CR is outside the replaced range.
3. Rebind with `create range boundDoc start 12 end 131`. The payload is 119 UTF-16 units (108 ASCII token units, 8 tabs, 3 CRs). Verify native start/end and exact grid readback, then recheck owned window/document and sentinel identities.
4. Invoke exactly once: `set diagTable to convert to table diagRange separator separate by tabs number of rows 4 number of columns 3`.
5. Read only: returned table bounds/dimensions/text; matching document-table ordinal; every document table; text before/after the new table; prefix, suffix and old-table bookmark text; all twelve cells in input row/column order. A complete ACK must show exactly 4×3, start 12, one new table, and every expected placeholder in its intended cell. It must show no REPLACE and no placeholder outside the new table.

```
WPSC_R1C1<TAB>WPSC_R1C2<TAB>WPSC_R1C3<CR>
WPSC_R2C1<TAB>WPSC_R2C2<TAB>WPSC_R2C3<CR>
WPSC_R3C1<TAB>WPSC_R3C2<TAB>WPSC_R3C3<CR>
WPSC_R4C1<TAB>WPSC_R4C2<TAB>WPSC_R4C3
```

The local Word.sdef command `convert to table` (sTXTwCtT) takes a direct `text range`, supports `separator` (code 5589, type WdTableFieldSeparator), row/column counts and returns a table. The exact enum is `separate by tabs`, code **0x02410001**, in **WdTableFieldSeparator / e177**. Verbatim command and enumeration excerpts are retained here as `command.xml` and `enumeration.xml`. Matrix04 established acknowledged conversion and correct location for an explicit range but returned 1×3 for one source paragraph. Four actual source rows with three nonempty fields each now supply the intended topology. The resulting 4×3 remains a hypothesis until the native/readback/XML gates pass.

The prepopulation is a real destructive edit to the task copy. Consequently, this proposal removes the previous ordinary -2710 catch: **every native error after submission, including -2710, -1708, timeout or invalid/missing ACK, propagates to quarantine and stops.** There is no automatic rollback, retry, repair, save or cleanup after an unknown result. Grid and partially converted states are preserved for exact-owned parent reconciliation. A complete `grid` row in the eventual batch ACK is evidence of the exact preconversion payload; on batch failure no such complete ACK is inferred. Native runner integration must retain the raw AppleScript log even when the partial state cannot be observed.

Outside XML expectation after a successful constructor is strict: original prefix paragraph; new 4×3 table replacing the REPLACE paragraph; only the precisely expected empty table-terminal paragraph; original suffix paragraph, old table, every original trailing paragraph and section properties in order. The three internal grid CRs must be consumed as table structure, not leak additional body paragraphs. Compare original outside nodes with the frozen fixture's normalization and exact terminal-CR exception; never delete/filter all empty paragraphs. Each old-table/prefix/suffix subtree and unrelated paragraph/section properties must remain equivalent, not merely have matching text. A subsequent authorized runner should save only the private test copy, retain DOCX/XML, apply these checks, then exact-guard close it and the sentinel.

The constructor-only diagnostic intentionally leaves its twelve synthetic placeholders in the test artifact so cell routing can be verified. Before any real semantic output or final fixture can be accepted, the filling layer must overwrite **all twelve** cells, including cells whose desired content is empty or omitted (the original native blank state must be preserved). It must apply the frozen cell string conversion, font/paragraph settings, width/border/header/split operations and input-order merges without adopting placeholder formatting or changing defaults. Verify no constructor placeholder survives in the final table or elsewhere. A future production token scheme must avoid collisions with legitimate input; these fixed ASCII names are only this synthetic diagnostic's data, not forbidden user text. Filling failure is a partial transaction and must stop/quarantine under the existing session rules rather than expose or save an apparently complete document.

Boundary: passing this experiment would establish only the intermediate 4×3 constructor and placeholder containment. Next is the already defined full semantic fixture with Unicode contents, dimensions/borders/header/split/merges, exact outside XML, terminal cursor/CR, PDF, save/close/readonly reopen and sentinel cleanup. Public middle-insertion support remains a separate open gate and is not reduced or silently removed.

Preparation verification: initial nine missing-module RED failures; 38 tests then passed (9 grid, 4 explicit-range, 18 conversion, 7 Matrix02). Tests cover actual 4×3 topology, exact 12:131 UTF-16 extent, single conversion ordering, removal of ordinary-error catching, twelve cell observations, wrong range/cell/dimensions and outside-placeholder/REPLACE leakage. The full generated script compiled with osacompile exit 0. Compilation is syntax evidence only. Source/test/script/SDEF hashes and the exact grid are frozen in preparation.json; source snapshots, test and compile logs are retained. No runner or native submission was added; root review and lease authorization are required before execution.
