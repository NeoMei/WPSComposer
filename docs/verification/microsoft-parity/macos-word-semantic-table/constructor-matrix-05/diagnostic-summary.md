# Matrix05 grid constructor — stopped on unacknowledged conversion error

The one authorized fresh-copy batch wrote a 119-UTF16-unit, nonempty 4×3 ASCII placeholder grid into the selected 12:19 range, rebound an explicit document range 12:131, verified its exact bounds and text, and attempted one `convert to table ... separator separate by tabs number of rows 4 number of columns 3` command. The native log reports -1708 at conversion: the explicit `text range id «data iWrg0C00000083000000»` of the owned document did not understand the command. The raw id encodes the requested 12:131 range.

There was **no complete batch ACK**. The first sentence describes the submitted sequential script and the reached conversion error, not independently acknowledged post-write state. Mutation may have occurred, as Matrix03 demonstrated; no resulting table topology, placeholder containment or unchanged outside content is inferred from the error. Matrix05 is FAIL pending separate parent reconciliation. No retry, third variant, save, close, XML gate, inventory, recovery or further AppleEvents ran after this failure.

This constructor has two writes (prepopulation then conversion), so every error propagates to quarantine; there was deliberately no ordinary -2710 catch. The synthetic placeholder grid and any partial table must remain in the owned diagnostic copy until root independently reconciles it. The Word lease was immediately returned. A bounded exact-owned/sentinel read-only reconciliation script is prepared, not executed, under `readonly-reconciliation-prepared/`.

Runtime evidence retains the precise owned path and sentinel 文档96/token in report.json/native_uncertainty. Raw log `failed-runtime/fdf7bad51f3b4d0e8ab9edfe581aa855.log` and its AppleScript, initial copied DOCX, complete source closure, SDEF and quarantine marker are retained. Only sentinel creation had a complete explicit observation ACK. No final DOCX/PDF/reopen/semantic acceptance is claimed. A parent reconciliation or cleanup will be separate evidence and will not retroactively turn this invocation into a pass.

Preflight used new isolated checkout `/var/folders/0n/49qgdd8x7kgcvh719fw743mh0000gn/T/wpscomposer-semantic-matrix05-o0mm28h9/checkout` at baseline 415c9d04f38e5d4a744baadce75a39bf76dc8cca plus retained overlays. The submitted template matched the approved bfcf1d24ba8c0b9a7cdb29a564b0e9713464fa44471955dd44318793b4132ade script exactly; probe dd5df01002d60fc1d71a946f66b83e177b0368ad0a08d145c0dcb97c9e827250 stayed frozen. Initial inventory was []; quarantine absent; frozen primitive/session/seed hashes matched.

Ninety-two offline tests passed (5 existing PyMuPDF/SWIG warnings), and the full script compiled with osacompile exit 0. To meet root's strict outside-XML gate, a diagnostic-only helper copied the reviewed fixture's normalized/terminal_paragraph/exact_splice functions without AST changes. Its nine new XML tests failed before implementation and passed afterward, covering extra/missing empty paragraphs, page breaks, section properties, misplaced cell text and wrong row counts. This saved-output gate was not reached in the native run. XML helper cfe123d3db7f5b7a56fd480764390122f9cec4df0d222309220bc2223b40fa85 and runner 953f2d78c413acf96e0d251fff232e7a0019c670f2ba806da7ef8ec2c799efb1 are retained separately from frozen production files.

No production/public/index change or placeholder-clearing implementation was made. Any later constructor clearing all cells (including ragged omissions), full semantic fixture, and public middle insertion remain separate reviewed work; this failure does not authorize a fallback or weaker support gate.
