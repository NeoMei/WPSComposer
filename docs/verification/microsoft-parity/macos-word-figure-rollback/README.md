# Figure rollback feasibility — bounded restoration verified

This is a build-only probe, with no production method or capability enabled. Its author stopped when the account quota was exhausted. Root reviewed the controlled fixture, fixed a NaN geometry false-pass with two failing cases, and ran14 pure/compile tests. Independent final review remains unavailable and is a required pending gate.

Native run01 fails its protective rollback-boundary check after inserting one actual inline picture. Native boundary03 records exact facts: expected rollback end12, suffix bookmark start10, picture10:11, suffix bookmark text slash+CR+space+right. Word expanded the bookmark at its start to include the inserted picture and paragraph; its start cannot serve as the unmodified-suffix boundary. The guard remains intact. The second-image failure/recovery branch is never reached. No rollback success or public figure acceptance is claimed.

Boundary02 separately retains a diagnostic-wrapper multiprocessing spawn/import error before insertion; the wrapper main guard is repaired and spawn-import checked before03. Root independently verifies every retained source/artifact hash, all original sources and scoped cleanup; final Word inventory is empty.

Next implementation: use acknowledged new-object bounds together with complete unchanged-prefix/suffix/content guards; restore the original bookmark graph from its exact preimage after owned rollback, then verify native saved document properties outside the selection. Do not simply remove the boundary guard or trust an expanded bookmark start. This needs independent source review, new native output/reopen and UI evidence before integrating the complete figure family.


## September 11 v2/v3 follow-up

V2 native returned nine scalar checks PASS for one-image rollback and rollback
after an actual second-image insertion failure. Root XML review then found
`w:noProof` newly present on both restored selected text runs. Retain that raw
PASS as scalar evidence only: exact full format restoration failed. The root
format review, XML diff and 12 verified source/artifact hashes are in `native-v2-01`.

V3 records the known selection's native no-proofing value before replacement
and restores it afterward. The fixture now compares complete `document.xml`
element structure/attributes/text, including all run/paragraph properties,
bookmarks and paragraph identities, plus exact style/theme bytes. Only the
explicit Word edit-session rsid and textId attributes are excluded; no proofing,
font or layout attributes are normalized away. Sixteen pure/compile tests pass,
including negative checks for the actual v2 defect, paragraph identity, size and
bookmark mutations. The native v2 artifact fails the new restoration assertion
before the fix.

V3 `native-v3-01` returns ten checks PASS, including native save/read-only reopen,
complete body/style/theme comparison, exact untouched unsaved sentinel, unchanged
input/resource bytes and empty final Word inventory. Root verifies all eight
source hashes and four original artifact hashes. PDF is one page; root visual
inspection confirms HEAD/left/replacement Chinese+emoji/right/TAIL with no
remaining picture. The parser splits the emoji in extraction; canvas rendering
is retained separately in the preview PNG.

This remains an owned controlled fixture with a uniform proofing preimage, not
arbitrary rich-text rollback or completed figure composition. Public methods,
column-container rollback, independent source review and full UI acceptance for
the finished feature remain open. No capability is enabled by this checkpoint.
