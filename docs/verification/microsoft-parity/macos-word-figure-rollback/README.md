# Figure rollback feasibility — unresolved

This is a build-only probe, with no production method or capability enabled. Its author stopped when the account quota was exhausted. Root reviewed the controlled fixture, fixed a NaN geometry false-pass with two failing cases, and ran14 pure/compile tests. Independent final review remains unavailable and is a required pending gate.

Native run01 fails its protective rollback-boundary check after inserting one actual inline picture. Native boundary03 records exact facts: expected rollback end12, suffix bookmark start10, picture10:11, suffix bookmark text slash+CR+space+right. Word expanded the bookmark at its start to include the inserted picture and paragraph; its start cannot serve as the unmodified-suffix boundary. The guard remains intact. The second-image failure/recovery branch is never reached. No rollback success or public figure acceptance is claimed.

Boundary02 separately retains a diagnostic-wrapper multiprocessing spawn/import error before insertion; the wrapper main guard is repaired and spawn-import checked before03. Root independently verifies every retained source/artifact hash, all original sources and scoped cleanup; final Word inventory is empty.

Next implementation: use acknowledged new-object bounds together with complete unchanged-prefix/suffix/content guards; restore the original bookmark graph from its exact preimage after owned rollback, then verify native saved document properties outside the selection. Do not simply remove the boundary guard or trust an expanded bookmark start. This needs independent source review, new native output/reopen and UI evidence before integrating the complete figure family.
