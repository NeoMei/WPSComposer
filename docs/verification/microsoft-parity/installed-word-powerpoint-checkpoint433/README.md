# Installed Word and PowerPoint checkpoint at 433a199

Both apps pass six public checks from the isolated installed bundle: create/close, file creation, edit/save, source preservation, native reopen values, and PDF text. All 214 copied source files match the installed bundle and repository after both runs. The preceding Excel smoke is retained separately. Personal installations are unchanged.

The first wrapper passes Word, then halts at its empty-PowerPoint precondition because 20 preexisting presentations are open. It does not attempt PowerPoint mutation. The separate followup preserves that entire redacted inventory and passes all six PowerPoint checks. Original wrapper failure and reports remain unchanged; assessment.json explains the scope. Word inventories are empty before/after, so Word sentinel coverage is not claimed here.

This is representative installed Mac smoke at this exact source checkpoint, not Windows installed acceptance, every public parameter/format or full parity certification.
