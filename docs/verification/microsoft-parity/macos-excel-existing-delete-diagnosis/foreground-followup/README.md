# Foreground isolated Excel process follow-up

This follow-up supersedes the earlier background-process feasibility conclusion, while retaining all earlier failures. Foreground `open -n -a Excel` produced responsive private instances; background `open -n -g` had previously returned silent no-ops for open/save/close and alert changes. The launch condition is observed; no deeper system cause is claimed.

Private PID 78330 changed only its own alerts, deleted the existing populated `Business report` worksheet, saved, reopened the exact file with its marker and two remaining sheets, closed and quit natively. The first roundtrip script reached save/close but failed an unparenthesized AppleScript count expression; the corrected independent reopen/cleanup result is retained separately. PID 79783 confirmed secure `open workbook ... with editable` after foreground launch without a document. Its first cleanup assumed the initial blank would remain; Excel had removed that blank. Independent empty-inventory cleanup then passed. Neither process remains.

A compiled OSAKit-language-instance transport preserves AppleScriptObjC JSON while routing Excel events to a PID. A dependency-free Python ctypes prototype matches that result. Both are diagnostic sources, not product transport; compiled executables are intentionally excluded. Raw Carbon OSA and default-target experiments remain retained as failed alternatives.

`private-json-roundtrip-03/report.json` passes the current public session open/save/close/reopen pipeline using that experimental transport. A bounded internal native deletion runs only in the private process. The saved file contains exactly `Data` and `Results`, and `PRIVATE-JSON-ROUNDTRIP` survives reopening; root separately checked ZIP sheet names and the shared-string marker. Source SHA-256 remains `afa1135ef9ed62cd07e18cb337a8383f3f5af894cc7c833db32ea1a059012156`. Private PID 1409 closes its owned books, restores alerts, quits and disappears; original PID 9212 retains both unsaved marker workbooks and alerts=true.

Run01 failed before launch because the diagnostic used the Excel dictionary name `rows` as a variable; the bounded trace and unchanged original markers are retained. Run02 reached deletion and native save but its missing Python main guard broke the spawned artifact validator. The parent separately verified its exact PID/start/path/saved state/sheets/marker, closed its sole owned book and quit; no publication is claimed. Run03 fixes only the fixture variable and main-entry issues. Original failure files are unchanged.

Production still rejects existing-sheet deletion. Before integration, the transport needs reviewed PID-reuse/executable binding, event-routing allowlists, absolute deadlines, launch/readiness ownership, failure quarantine and exact close/quit semantics. The passed prototype does not satisfy these production requirements, attached-session parity, Windows native acceptance or release approval.
