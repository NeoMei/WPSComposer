# Frozen private Excel regression — round 42

Immutable snapshot `43ae6c43b8117c5e3b40d4c43f91f4a7257d4cfe` passes **5,170 tests / 12 skips** in 224.96 seconds. All 392 tracked source/configuration hashes remain equal before/after and to live. `snapshot.json`, `source-hashes.json`, `pytest.log` and `pytest.xml` retain the evidence. Native gates remain opt-in skips, not native passes. A private cloned checkout with npm dependencies isolates the regression from working-tree and native UI activity.
