# Hosted regression at 73a00e1

Pull-request run [34498585408](https://github.com/NeoMei/WPSComposer/actions/runs/34498585408) succeeds at `73a00e1f0a79d038d8dcf3402057700ada5f27e9` on all four jobs. Ubuntu Python 3.9 and 3.12 each pass 5,221 tests with 44 skips. Windows Python 3.9 and 3.12 each pass 5,167 tests with 98 skips. Raw logs, JUnit, run/job metadata, artifact metadata and extracted-file hashes are retained here.

The earlier push run `34498579108` for the same head was cancelled after the pull-request run took precedence; its metadata is retained separately and is not counted as a test result.

This is portable-suite evidence for the exact candidate, including the new source-only native-heading feasibility fixtures. Opt-in native Office tests are skipped by these runners. Hosted success does not certify Office/WPS native behavior, UI behavior, installed bundles, full parity, or release readiness.
