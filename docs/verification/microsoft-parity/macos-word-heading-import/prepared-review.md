# Native Flat OPC input import — source-only review package

2026-09-10. No production edits, Office execution, script compilation, installation, commit or push. The absent `MacWordSession.add_heading_level_native` remains absent; method counts and capability certification are unchanged. Existing run-01 through run-05 evidence is untouched.

The new hypothesis is that Word's native file importer can materialize a distinct Normal-based style from an input Flat OPC fragment carrying the exact `rPr` and `pPr` copied from Word's own saved Heading 1 style. This avoids the unexposed AppleScript complex-script-size setter and the already-failed formatted-text/style-redefinition path. The XML is an input fragment only; Word must import it into the bound document, create its actual native style, edit, render, save DOCX/PDF and reopen. This is not an implementation of the public method or proof of its selection/cache/numbering/failure-order contract.

The current installed Word.sdef SHA256 is `7cb51b924cab566320cc3019e92c1516302ae11220a81ae6279f1930c26320e7`. It declares `insert file` (`sWRDwiFl`) with a `text range` target and file name, `format flat document` (19), and `open format xmldocument serialized` (14). It has no `InsertXML` command. The probe deliberately uses only the actual `insert file` command. Whether this command's format auto-detection accepts Flat OPC is an explicit first native hypothesis, not inferred from generic XML support. Microsoft's OOXML guidance describes Flat OPC packages, native style definitions, style conflict behavior, and preservation of `szCs`; it supports the package design, not this particular AppleScript entry point. [Microsoft OOXML insertion guidance](https://learn.microsoft.com/en-us/office/dev/add-ins/word/create-better-add-ins-for-word-with-office-open-xml).

The probe seeds only an owned document and independently guarded unsaved sentinel. It saves `source-preimage.docx` through Word, resolves unique document-local Heading 1 and Normal IDs, requires an explicit source `szCs`, and rejects name/ID collisions. It copies complete native source `rPr`/`pPr`, actual Normal and document defaults into one new custom paragraph style. Source Heading 1 identity/linkage is excluded. The UTF-8 Flat OPC input has explicit package/document/styles relationships and no external links, macros or executable content. Existing owned document bytes are never rewritten with XML.

The one native import replaces paragraph 2's full `REPLACE` range, including CR. Strict native readback must equal the UTF-16 start/end coordinates and exact entire SOURCE/IMPORTED/SUFFIX text, so append-only insertion or a missing/extra paragraph fails before style acceptance. Both malformed source-font readback and missing `szCs` stop before import. The report retains raw failed rows and staging; no alternate importer, retry or fallback is automatic.

Subsequent gates require all 65 actual font observations, the complete existing font/paragraph oracle, exact complete saved style `rPr`/`pPr` including `szCs`, and preservation of every existing style definition (only `rsid` ignored). A fresh paragraph uses only the imported style after clearing direct formatting. A native source-font-size change must leave both imported style XML and fresh paragraph appearance unchanged; the source is restored and its complete original definition is required afterward. Word saves DOCX/PDF and read-only reopen repeats font/paragraph/fresh-style checks. Source-preimage and input bytes must stay unchanged; root still inspects the rendered PDF for Arabic and CJK font/layout evidence.

Cleanup uses the existing independently reviewed owned-close → exact sentinel inventory → guarded sentinel-close sequence. Raw sources, dictionary hash, report rows, input XML, source snapshot, intermediate/final Word-saved DOCX, PDF, rendered pages and runtime staging are retained. Failure before or during import is not evidence against the later style-cloning hypothesis; the phase is recorded. Success is restricted to this seed and native import mechanism; complete inherited/theme/linked-style behavior, existing detached-name conflicts, native numbering lists and arbitrary bound-selection insertion still need independent work.

Files frozen for initial review:

- `fixtures/microsoft_parity/macos_word_heading_import.py`: `0e41b5921857b9221af96e8c57a53994580192414025b8e9af2e5d482dfa489e`.
- `tests/msoffice/test_macos_word_heading_import.py`: `aea51cba47212cfef10b6423a0663c47971a3da69a501d9688cd93a10cd26eb3`.

TDD: 7 failures for absent fixture, then 7 passed; added malformed-preimage pipeline test, observed 1 failure/7 passed, then 8 passed in 0.22 s. Related source-only regression: `.../WpsComposer/.venv/bin/python -m pytest tests/msoffice/test_macos_word_heading_import.py tests/msoffice/test_macos_word_heading_feasibility.py tests/msoffice/test_windows_numbering.py -q -k 'not compile'` → **52 passed, 8 deselected in 0.54 s**. The excluded tests are script compiler tests; no native app or `osascript` was invoked. Review is assigned to the parent’s independent reviewer. Root must compile the emitted native phases and inspect any syntax failure before the one opt-in execution.

After review and under root's serialized Word lease only, run `/Users/neomei/项目/codexprojects/WpsComposer/.venv/bin/python fixtures/microsoft_parity/macos_word_heading_import.py --execute --output <new-private-directory>`. Omitting `--execute` exits 2 before output creation; an existing output directory is rejected. The fixture never updates capability records or public APIs itself.

Independent review identified a real oracle gap: fresh-paragraph scalar readback could not observe direct `szCs` or paragraph overrides. Added a saved-package gate requiring exactly one FRESH paragraph, its actual unique imported-style ID, no linked source style, and no direct run/paragraph-mark/paragraph appearance properties. It runs before source challenge, after challenge, at final save and against the immutable package after reopen. Wrong style, direct `szCs`, direct indentation, paragraph-mark font overrides and duplicate FRESH paragraphs are rejected. The existing complete imported-style XML and independence checks remain unchanged.

This correction had RED **6 failed, 8 passed**, then source-only related GREEN **58 passed, 8 deselected in 0.34 s** (14 tests in the new file). Refrozen for re-review: fixture `5d300e46d97b2d81a7a7b9c4cdcaf09304aed4b8927d565773686a59929b355c`, tests `7a2a6bf00a0314ed5a18531e1ac1d7af3884b4f5bf49191cc2b98355b070d9a6`. Native execution remains HOLD pending parent review/compilation and lease.

Independent re-review completed on these exact hashes: **READY for parent-controlled native hypothesis**. Reviewer independently ran the 14 pure tests (**14 passed in 0.33 s**) and confirmed the fresh-paragraph XML issue resolved. Reviewer made no source edits or native invocation. Native script compilation and actual Word execution remain root-owned next steps; no full direct-heading implementation or acceptance claim follows.
