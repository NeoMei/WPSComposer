# Word native import feasibility — not accepted

The new Flat OPC input carries complete Word-saved source style properties for a detached style, including complex-script size. It is an input fixture, not a document output generator. The public heading method remains absent.

Run-01 fails at native `insert file` with -1708, before any import acknowledgement. The following native close also returns -1708. This does not prove Flat OPC itself is unsupported. Source-preimage content/hash and saved state match the unchanged four-paragraph document. The exact owned URL and synthetic sentinel were subsequently closed through Word UI; independent inventories are empty and guarded quarantine recovery succeeds. Original FAIL, runtime, failed cleanup attempt and separate successful parent-cleanup report remain unchanged.

That run also exposed a latent runner assumption: Word retains an additional terminal CR. The corrected source-only oracle snapshots full actual native text, validates exact target and UTF-16 boundaries, then computes a splice preserving all outside text. Its 25 focused tests pass. This fixture-only repair is not a native success.

The separately reviewed DOCX-control-01 uses one donor saved by Word, the same insert command/range/flags and a fresh recipient. The call succeeds but inserts the donor after REPLACE rather than replacing REPLACE. The strict replacement check correctly fails. Both donor and recipient owned-close, unsaved-sentinel preservation and sentinel cleanup checks pass. This establishes a native DOCX insertion primitive only; neither native style cloning nor the public heading contract is certified. No automatic retry/fallback or production capability was added.

Compile-01/02 and raw source-bound reports are retained. Build-only scripts retain original paths; reproduce them under build/word-heading-import-20260910 and use new output directories. Full44/45 portable results remain separate from these native failures. The next design must separate format recognition, exact insertion coordinates and source-independent style materialization.

The subsequent collapsed-DOCX control passes 19 native checks: insert at exact original start, confirm full text/native bounds and named-style/inventory preservation, clear only the shifted REPLACE paragraph, save and exact cleanup. Parent independently verifies final DOCX body equals the expected UTF-16 splice. Source and donor bytes, original styles and unsaved sentinel are preserved. This proves a disposable owned-document text/range primitive; outside direct formatting, complete detached-style materialization and attached rollback remain unverified.

Explicit-converter Gate A (`flatopc-open-01`) fails at the post-open exact identity assertion with `WPSC_FLATOPC_IDENTITY_DELTA` (-2700). The open command itself returned, but no valid owned-document acknowledgement was obtained. Independent native inventory and Word UI then show no open document; the input hash is unchanged and no converted DOCX exists. `flatopc-open-01-parent-cleanup` separately verifies empty inventories and guarded quarantine recovery. The original FAIL remains unchanged. This does not establish format recognition, style materialization or heading support.

The one-shot direct-POSIX-file variant (`direct-open-01`) retains a separate FAIL: the subsequent `openedValue` read raises -2753 (undefined variable), so no structured open observation is produced. Independent inventories remain empty, input bytes remain identical and guarded quarantine recovery succeeds in its separate parent-cleanup report. This neither establishes the converter's return value nor native input recognition. Further XML-open retries are stopped; the next distinct diagnostic will package the same complete style parts as DOCX input and require actual Word open/save/reopen and style equality.

The distinct DOCX-packaged-input Gate A (`docx-package-01`) passes **23 checks**. The same four XML parts plus OPC content types are opened as a private DOCX copy by Word. Explicit native saved=false/save/saved=true acknowledgements and changed output bytes demonstrate serialization. Word-saved and read-only-reopened packages preserve the exact complete source `rPr`/`pPr`, including `szCs=48`, in a unique Normal-based detached style; the sole paragraph uses that style with no direct formatting overrides. Native readback agrees on text/style/base. Original input, source parts and output survive close/reopen unchanged, with final empty inventory. No source theme part is supplied, so resolved appearance, imported recipient semantics, source independence and public heading parity remain unproved.

A separate parent package comparison finds Word's generated `word/theme/theme1.xml` byte-identical to the original seed's theme (`7017cbe8b7512b56e04e6b3e3579aa862a35b6f9f31e0bf48328f6e5c074ebbd`). This closes theme-file equality for these two exact fixtures only; it does not establish arbitrary inherited appearance or recipient behavior.

Integrated donor-to-recipient run01 reaches exact collapsed insertion, guarded replacement, source/Normal/default/theme matching, original-style preservation with only the exact complete clone added, and all65 native font observations equal. It then **fails** in the inherited paragraph-read oracle: Word rejects `count tab stops of paragraph format of Word style` with -1708. No complete paragraph, fresh-style challenge, PDF or reopen success is claimed. The owned context closes, unrelated synthetic sentinel is verified and closed, and the final independent inventory is empty. The raw FAIL and source freeze are retained. Next diagnosis isolates the tab-stop read interface; the successful earlier gates do not overwrite this failure.

The read-only tab diagnostic (`tab-list-read-01`) retains an overall FAIL for style collection access. The separately attributed actual-paragraph path passes: source and imported paragraphs each expose19 equal tab stops with matching position, alignment, leader and custom/default flag. Each block preserves saved state, full text/topology, inventory and input/private bytes; the exact copy closes with empty final inventory. This supports effective paragraph-tab checking for the exact applied-style fixture; it does not establish direct access to an unused style's tab collection.

Integrated donor run02 uses separately attributed style scalars and effective paragraph tabs, preserving all complete named-style/XML gates. It passes those checks, exact replacement and original-style preservation, then **fails** the fresh-paragraph font assertion: four whole-range family getters (`ascii name`, `complex script name`, `name`, `other name`) return empty text where the source style getter returns Arial; the other61 properties match. The fresh-style challenge, final PDF and reopen are not reached. Owned/sentinel cleanup succeeds and final inventory is empty. Mixed-script aggregate range semantics are a diagnostic hypothesis, not an accepted reason to treat unknown font names as equal. Raw FAIL is unchanged.

`font-anchor-read-01` completes13 read-only diagnostic checks. Exact UTF-16 Latin/CJK/Arabic/emoji anchors show that source and clone styles expose the same names but source and imported text ranges do not always do so. Parent OOXML inspection finds direct `rFonts` overrides on the source seed's Arabic (Times New Roman) and emoji (Apple Color Emoji); the imported paragraph is style-only. Thus neither whole-range empty names nor source/clone character differences may simply be normalized away as equivalent. Fresh paragraph creation order and reset behavior require a separate controlled check. All diagnostic state/byte/ownership cleanup checks pass; no fresh-paragraph or appearance acceptance follows.

`fresh-order-01` saves trial A's native result, then fails its strict all-style-definition preservation check; B is not executed. The fresh paragraph has the intended paragraph style and only one direct font override, Apple Color Emoji on the emoji run. Parent XML comparison finds no added/removed styles and exactly one existing-definition change: `Default Paragraph Font` loses `semiHidden` after the combined text/style/reset sequence. The exact triggering operation is not yet isolated. Independent final inventory is empty, input is unchanged and no quarantine remains. No A/B success or font-reset repair is claimed.

The distinct B-only empty-carrier diagnostic (`empty-carrier-01`) completes **23 checks**. Applying the clone to the exactly empty final paragraph before inserting text, without resets or an extra paragraph, preserves every named style definition and the paragraph count. Latin, CJK and Arabic anchors match the existing imported paragraph. Word adds an eastAsia font hint to CJK and Apple Color Emoji family overrides to the emoji; `fresh_style_only_xml` remains false. This does not accept full appearance or heading parity. It avoids the style-definition side effect seen in combined trial A, without isolating which A operation caused it. Exact source bytes, owned close and independently empty final inventory/quarantine checks pass. A same-text native source-style control and subsequent source-independence challenge are still required.

Same-text native control run03 passes53 checks, then remains **FAIL** at its strict source-challenge XML gate. Identical new text on source Heading1 and the detached clone has exactly equal native direct-run formatting, all65 font observations, per-script anchors and19 effective tabs. Changing source size17→28 and paragraph spacing24→31 changes the source control while clone/fresh values remain17/24. The full-tree assertion correctly reports an additional native change: the reciprocal linked source character style (sourceid1↔characterid10, “标题 1 字符”) also changes size34→56 half-points. Parent confirms these are the only three attributes changed, with full cleanup, immutable inputs, empty inventory and absent quarantine. The original FAIL is retained; restore/PDF/reopen are not reached. A new v4 oracle must explicitly guard the reciprocal typed link and exact preimage before expecting this native pair update; arbitrary style changes remain failures.

Run04 explicitly guards the source paragraph/linked-character pair and passes its three-attribute full-tree challenge plus53 earlier checks. It then remains **FAIL** on unsupported control content. Native spelling checks inserted balanced spellStart/spellEnd markers around العربية and split the surrounding identical-format run in both controls. Parent independently verifies exact per-character direct formats/text are unchanged, along with cleanup and immutable input hashes. Restore/PDF/reopen are not reached. V5 must validate the observed proof-marker grammar and compare exact character formats across equivalent run segmentation, retaining strict rejection of real formatting changes and unknown content. Original failures remain immutable.

## Same-text control v5 and separate visual failure — September 11

`native-donor-import-05/report.json` retains raw **PASS, 96 native checks**: matching mixed-script controls, complete style import, 65 font observations, 19 effective tabs, source-plus-linked-style size/spacing challenge, clone independence, exact full-style restoration, DOCX/PDF and read-only reopen. V3 and V4 failures remain separate. V5 validates balanced empty spelling annotations while comparing exact per-character formatting and complete paragraph properties. Its 19 pure tests pass.

**Rendered artifact gate FAIL.** Root inspected the PDF page and exact Word UI copy: the Arabic word is seven boxes in each paragraph. Installed Arial Bold Italic faces have no Arabic mappings and the embedded PDF font contains only `.notdef`; the read-only diagnostic finds 21 glyph-ID-zero occurrences and 21 NUL extraction characters. The new negative guard passes five pure tests and correctly rejects this PDF. This guard is necessary, not sufficient for complete script shaping/layout acceptance. No font fallback is enabled.

`ui-05/report.json` records inspection of the exact saved Word copy and explicit close, followed by independent empty native inventory. The copy equals the original DOCX byte-for-byte and the source remains unchanged. This close-only continuation does not independently certify the earlier edit/Undo/reopen sequence. Complete UI editability acceptance remains separate. Neither result enables `add_heading_level_native` or establishes arbitrary inherited-style parity.

Fresh UI continuation: `ui-05/fresh-ui-report.json` records a newly performed, complete edit → Undo → explicit Save → close → same-path native UI reopen → close sequence. Inline CUA screenshots visibly establish fourth-paragraph `FRESH` → `UI-CHECK` → `FRESH`, restored text after reopen and saved status. Source and copy SHA-256 remain equal, independent final Word inventory is empty. The earlier close-only report remains unchanged. Arabic rendering still fails in the fresh canvas observation.

Italic-only run01 retains a strict-validator FAIL before PDF export: native source/linked italic reads false, and Word saves false by removing exactly the two source/linked `<w:i/>` leaves instead of emitting `w:val="0"`. No other styles-tree difference was found. Parent checks preserve the pinned input, all22 source hashes at the run, and empty final inventory. No PDF, restoration or visual improvement is claimed for this failed trial. The next distinct continuation will open the pinned saved off-state, validate the precise inherited false state and test export/restoration.

Italic continuation01 completes26 native checks using the pinned off-state. Source-only nonitalic text renders Arabic connected glyphs with zero glyph-ID-zero errors; the two unchanged italic controls still have seven missing glyphs each. Restoring source italic reproduces its seven missing glyphs. Complete styles/theme bytes restore exactly, all four paragraph character properties remain intact, saved DOCX reopens, sources/artifacts match and final inventory is empty. This identifies a font-face rendering dependency, not a usable fallback policy or full heading acceptance. Both PDF pages are retained and visually inspected by root.

## Production-shaped style definition install/refresh boundary

Two distinct native runs fail **before any input import**. Run01 saved a reversed source inheritance chain despite a literal setup acknowledgement. Reviewed v2 discards the make-return reference, resolves the exact named parent, and checks actual typed native base-style/name values plus saved XML. Run02 fails its first parent-base assertion: attempting to base the custom parent on Normal leaves its base at Heading1. The source-to-custom-parent hypothesis is never executed, and same-name definition installation/refresh remains untested. No further setter normalization is inferred to work. Both raw failures, saved setup artifacts, all23 source copies per run and exact unsaved-sentinel cleanup are retained; root confirms empty final Word inventory.

The broader heading path now needs a supported native style-definition mechanism before this dependency can advance. These probes concern persisted definitions only; they do not prove applying the target style to existing/fresh paragraphs.


## Single-style Organizer follow-up

The distinct [Organizer probe](../macos-word-organizer/README.md) now passes19 native checks for first installation and same-name17→23pt update. Native live values change before the private disk file; explicit native save persists the update. Complete non-target styles, theme, body/bookmarks and selection are preserved, a separate unsaved sentinel remains unchanged, native reopen succeeds, and final inventory is empty. Root verifies16 source/artifact hashes and4 pure/compile tests. The prior setup/native failures remain immutable. This is a successful copying primitive; arbitrary inherited heading styles, existing/fresh paragraph application, numbering, Arabic rendering and public method integration remain pending.
