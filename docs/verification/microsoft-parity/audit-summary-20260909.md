# September 9 comprehensive audit checkpoint

This candidate remains incomplete and is not a release approval. The unchanged frozen baseline contains 628 required rows across two target platforms. None is waived. Twenty-one of eighty Mac Word direct methods remain absent after object, section, field/index and reference/bibliography slices; full argument/format/active-lifecycle parity and Windows candidate native execution remain open.

The audit has closed source-publication preflight, lock cleanup, Excel explicit clone-to-end and logical SaveAs/current/copy, PowerPoint logical save and background append, Windows public business DTO/tuple/closed-field transport, Word section metadata/numbering, field snapshot identity and index verification, and Excel output-keyword issues. Word logical-save's fingerprint-error recovery issue is independently closed; saving-close retains its open session lock until retry or explicit discard, as required by the established lifecycle contract.

Full round 15: 4,037 passed / 12 skipped. Round 16: 4,039 / 12. Round 17: **4,098 / 12** in 211.73 seconds. Native opt-in/passive-bridge skips are not native passes. Integration round 2 publication and Excel close findings are independently closed after validator-error/deadline, partial-group publication, same-inode content and symlink-path replacements were reproduced and fixed. The parent reran 136 targeted tests. Final-source Excel `integration-round2-02` passes eight native checks, with source, artifacts, actual recovery text and inventory independently verified. Isolated installed-audit-05 subsequently passed three six-check app flows with 118 source-equal shipped files at its freeze. The later Windows proxy/worker repair changes that source identity, so installed-audit-05 is not final-candidate installation evidence.

Representative native evidence includes Word section/field/logical-save outputs, Excel logical-save/clone, PowerPoint logical-save/foreground append, actual Word UI edit/Undo/save/reopen, and private installed-audit-04 three-app six-check flows with 117 matching shipped files at that freeze. WPS presentation inspect/edit/reinspect/PDF and Writer inspection pass after isolated startup; WPS spreadsheet registration is separately failing before its profile is loaded, with source and existing process preserved. Original failures are retained with separate corrected runs.

Windows Remote Control exposes the saved project but the current task still yields an old interrupted prototype turn. No new candidate command execution, Windows full suite, native three-app output, UI or installation is proven. The existing draft PR is #9; the last pushed commit before this audit remains 428283e. No new release, merge, tag or personal installation has been performed.

The five-method Mac Word reference/bibliography slice has 217 focused tests and final-source native `macos-word-references/run-04` passing eight checks. Three real-host failures were fixed: missing-value empty ranges, unsupported implicit tab collection iteration, and fallback formatting leaking to suffix text. Earlier failed reports and exact cleanup/diagnostics remain preserved. The parent independently checked twelve source hashes, both artifacts, four REF and three SEQ fields, exact styling and the PDF visual. Scoped independent review reports no new P1/P2. The real controller checkpoint/rollback recovery chain remains unimplemented and is not counted as native verified.

Round 3 integration review reproduced Windows saving-close prematurely terminating a recoverable worker, and premature native-handle registry clearing. Repairs are in progress; the acknowledged save failure must retain an open session and handles, while native-close uncertainty still quarantines. A source-bound full regression and isolated installation will follow the frozen repair.

`ui-word-references-01` now records actual title editing, Undo, explicit Save, close, reopening the exact disposable document through Word recent files, visual confirmation and final close. Its saved artifact text and field instruction nodes match the original; the original SHA256 is unchanged. A native-open-panel follow-up did not open the file, so the observed recent-file route is recorded rather than claiming that action succeeded.

A new remote continue request was accepted, but a fresh wait still returned revision 27 and the old interrupted turn. No new command output or Windows acceptance is inferred from accepted dispatch.

The Windows round-3 repairs are now independently closed, including read-only exact binding verification within the original deadline before any `session_state: open` claim. Word interrupted-transport recovery is also closed by scoped review and independent cancellation regressions. Final round 19 passes **4,112 tests / 12 skips** with all source/test hashes unchanged across the run. Fresh isolated `installed-audit-07` binds 118 matching shipped files and three passing six-check native public app flows.

Task review remains partial across all six original tasks (`task-audit-20260909.md`). The new table-only checkpoint probe establishes that range deletion clears cell contents but preserves the native table skeleton; repeated range deletion is not a valid rollback. That native implementation gap remains open, with raw geometry retained. No comprehensive parity or release approval is claimed.
