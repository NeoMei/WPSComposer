# Public private Excel lifecycle acceptance

Runs 01–03 are retained failures before task-document opening. Run 01 exposed ordinal workbook inventory requirements. Runs 02–03 exposed an AppKit PID lookup returning nil while libproc still returned the exact live birth and executable; run 03 records the exact failing lookup branch. Run 02 also exposed diagnostic code overwriting a captured successful command reply with the post-check exception’s empty stream defaults.

Each failed run preserves source and original unsaved sentinels. Parent cleanup separately verified the exact test-owned process and pristine bootstrap workbook before close/quit, proved the recorded birth gone, and invoked guarded quarantine recovery. Retained failed workspaces were not deleted. None of these failures are public operation acceptance. Historical scripts bind run-specific paths and processes and must not be blindly replayed.

Run 04 passes the repaired production path: public `open_document(engine="msoffice")`, existing populated worksheet deletion, write, save, PDF export, close/quit, and a separate public read-only reopen all succeed. Both exact private births exit. Original unsaved workbooks, alerts and source bytes remain unchanged. The four captured production hashes and runner hash remain equal after the run. This closes a representative owned-file Excel operation chain; active-document deletion and full argument/platform/release gates remain open. The launch FFI still lacks a hard external watchdog and is the next lifecycle requirement.

UI01 verifies existing Results!A1 edit → Undo → explicit save → close → same exact file reopen, plus Data!Z40 persistence. All saved cell values/formulas match run04, original output bytes remain unchanged, and a PID-bound final inventory confirms both original unsaved sentinels/alerts unchanged. The initial Excel open-panel attempt left Open disabled and was cancelled; actual opens used Finder’s explicit Open With Microsoft Excel. Root viewed PDF page 1 with its table, merged row, numeric formatting, header and chart; this is a representative visual check only.

Auto01 independently exercises public `edit(engine="auto")` with remove-existing-sheet + cell-write, XLSX output and PDF export. Both operations and publication succeed; saved sheet names, marker, original source, original unsaved sentinels and six current production hashes are checked. Automatic routing uses the owned-file capability, while active attachment still rejects unverified existing-sheet deletion in pure preflight.
