# Remaining Mac Word direct method surface

This static inventory follows the frozen 80 Word direct-method rows. With the staged four anchored-quality methods, 71 names are declared by MacWordSession and 9 remain absent. The four newest names still require native acceptance and have explicit position/object gates. Declaration is not native verification or complete argument support. Equivalent behavior exercised through the generation pipeline does not certify the same direct session method. Shared lifecycle, property, format, target and verb rows remain separate.

The baseline is unchanged. These are implementation gaps, not waived requirements:

- `add_captioned_figure_fallback`
- `add_captioned_figure_native`
- `add_equation_native`
- `add_equation_native_fallback`
- `add_heading_level_native`
- `add_horizontal_line`
- `add_semantic_table_fallback`
- `add_semantic_table_native`
- `add_wordart`

The newly declared style methods still reject character styles and unsupported properties explicitly. Representative new direct-method typography, section and field native acceptance is recorded in `macos-word-business/resume-03` and `macos-word-business-followup/resume-03`; complete argument combinations and attached-session behavior remain unverified. The Office.js supplement and Windows results do not automatically close these method rows.

The five object methods have a reviewed implementation and representative native acceptance in `macos-word-business-objects/run-05`: 21 named checks, including a nonempty unsaved sentinel, source-preserving reopen and native artifacts. Full argument parity remains open: floating-image alt text, unverified wrap values and floating image blocks are rejected before staging; raster variants not exercised natively are not certified.

The five section/metadata methods have independently reviewed implementation and representative native acceptance in `macos-word-sections/acceptance-07` and `variants-03`. Checks cover saved/reopened section geometry, PAGE continuity and formats, metadata coercion, and an unchanged synthetic unsaved sentinel. The nine field/index methods subsequently passed independent implementation review and representative native acceptance in `macos-word-fields/run-12`. Direct figure/table placeholder helpers retain their frozen placeholder behavior; they are distinct from native filtered indexes. Real tracked TOC growth preserves following REF semantic identity. Full story/argument and attached-session coverage remains open.

The five reference/bibliography methods have 217 focused passing tests and representative native acceptance in `macos-word-references/run-04`. Exact native REF refresh, bibliography geometry, list tabs, occurrence styling, local partial-failure rollback and source-preserving reopen passed. Independent scoped review reports no new P1/P2. At that earlier reference checkpoint, real controller checkpoint/rollback remained a dependent gap; the subsequent limited controller acceptance is recorded below. The short reference fixture does not prove multiline/page-boundary behavior or full argument parity.

Continuation: checkpoint/rollback now have independent scoped review and run-05 source-bound native acceptance (13 checks), plus UI edit/Undo/save/reopen. Append-only text/table/field restoration is supported; changed drawing objects are rejected before rollback/fallback, so figure/equation recovery stays open. Inline/block notices pass independent review and final native run-03 (57 checks across six scenarios), including actual partial-table rollback before a single fallback. Pagination run-01 passes 11 native checks, including three-page geometry, readonly reopen and unsaved-document preservation. Paragraph-rule production review passes; run-02 proves native/XML/reopen behavior, and separate offline-correction-02 validates its retained PDF after the detector repair. Original failed runs remain unchanged. Notices and paragraph-rule UI edit/Undo/save/reopen checks pass. The subsequent destructive rollback error-isolation repair passes scoped review, round-24 regression, recovery run-06 (13 checks), notice run-04 (57 checks), and genuine injected-error run-02 (14 checks). Shape/equation recovery and arbitrary middle insertion remain separate gaps.

The numbering slice adds `add_equation_number_native`: formula text at the actual bound selection, global/chapter SEQ/STYLEREF fields and number-only bookmarks. Independent re-review passes 395 focused tests after fixing cross-story identity collisions, creation-order stable ordinals, malformed acknowledgements and pre-submission quarantine. Native `macos-word-numbering/run-01` passes 10 checks with all 125 source hashes unchanged, including noncollapsed middle insertion, references, append rollback and save/reopen. `ui-word-numbering-01` proves actual existing-text edit/Undo/Save/close/exact-path reopen, unchanged file/XML bytes and final empty native inventory. OMath and general middle rollback remain separate requirements.

The staged four anchored-quality methods have not passed native acceptance. First-stage `macos-word-quality/run-01` failed the read-only first-upsert table-position check after successful bookmark reservation. `target-read-probe-01` isolates collection-item reference resolution; ordinal lookup with the same comparison succeeds. The raw probe remains FAIL because its diagnostic expected-outcome assertion stopped the run. Both independent parent cleanup reports verify empty document inventories and guarded quarantine recovery. The reviewed resolver repair and a fresh native stage-one run are separate subsequent gates.
