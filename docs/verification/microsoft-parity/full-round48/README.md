# Full regression round 48

**5,281 passed / 12 skipped** in 221.49 seconds, exit 0. Run from immutable clean checkout `c454ac8db9b46ea3fd9eaf64a9fcd55d8939856b` (parent `ce322b29cef43df68551e387dcc4e6e94bab5495`). Snapshot creation used a temporary Git index and included untracked source/test/fixture files. The real index and branch were not changed by snapshot creation.

All 402 recorded source/configuration hashes match before, after and the live worktree. The checkout had no ignored `build/` reports, proving caption tests do not depend on local acceptance output. npm ci ran before the full pytest invocation; logs, JUnit XML, snapshot metadata and hashes are retained here. Twelve opt-in/passive native skips remain skips, not native passes. Native Word caption run04 and UI04 are separate evidence. This result does not close the full parity or Windows native release gates.
