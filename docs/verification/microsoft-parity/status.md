# Microsoft parity candidate status

This is an implementation checkpoint, not a release approval. The frozen baseline is `baseline.json` (628 required capability rows at v0.9.0, commit 6dd3a00). Raw COM access is recorded separately and is not a semantic parity requirement. No baseline row has been removed to make the candidate appear complete.

## Current continuation checkpoint — September 10

The private Excel lifecycle now passes [public native run 04 and UI acceptance](macos-excel-private-public/README.md): open an existing workbook in an exact owned process, remove a populated worksheet, write/save/PDF, close/quit, then reopen in a second private process. Original unsaved workbooks and source bytes are preserved. A traced AppKit class-lookup nil was repaired by retaining the launch handle while still verifying live kernel birth/path and bundle identity; diagnostic cancellation and post-check reply preservation also have regressions. Exact dead-process quarantine recovery preserves failed stages. Automatic file-edit engine selection now accepts this operation while attached deletion retains its guard. The blocking native launch FFI still needs an external deadline watchdog; the public result is scoped acceptance, not release approval.

[Full42](full-round42/README.md) passes **5,170 tests / 12 skips** in 224.96s from immutable `43ae6c43b8117c5e3b40d4c43f91f4a7257d4cfe`; all 392 recorded source/configuration hashes remained unchanged and match live. Real UI acceptance edits existing Results!A1, undoes to Item, saves/closes/reopens the exact copy and verifies Data!Z40. Disk values/formulas and the original source/sentinels are preserved. Windows native, remaining Word quality/semantic rows, hard launch timeouts, final install and whole-parity certification remain open.

The [four-fixture page-story matrix](macos-word-quality/page-story-matrix-01/README.md) preserves state and repeated typed reads, but the linked multi-section case returns an ordinary native error and incomplete required story coverage. It therefore does not close Word quality snapshots. Windows continuation was accepted again; latest compact state still exposes the old interrupted turn, without current-candidate command evidence. Full parity and release remain blocked.

[Hosted CI10](portable-ci/run-10/README.md) succeeds on `6653e14` in all four jobs: Linux Python 3.9/3.12 each **5,027 passed / 44 skipped**; Windows Python 3.9/3.12 each **4,996 passed / 75 skipped**. A current-output [Excel UI copy](macos-excel-save-ack/ui-01/report.json) passes actual edit/Undo/Save/close/reopen with independently checked disk contents, unchanged source and original unsaved sentinels. The three-fixture [Word defined-style matrix](macos-word-quality/defined-style-read/matrix-01/README.md) passes repeatability and state preservation, including an unused modified built-in. Linked character-style/native-vs-OOXML property coverage remains incomplete; this does not close full quality or parity.

The [strict Excel save/close acknowledgement repair](macos-excel-save-ack/README.md) passes independent review, 109 focused tests, and three current-source native write/save/reopen cases with exact cleanup and source/sentinel preservation. [Full41](full-round41/README.md) passes **5,059 tests / 12 skips** from immutable `2775c23cb17a476394910099fa3197c3fe63078b`; all 386 recorded hashes remain unchanged and equal to live. The earlier hosted CI result does not cover this new repair.

The [foreground private-process follow-up](macos-excel-existing-delete-diagnosis/foreground-followup/README.md) now demonstrates existing populated-sheet deletion, durable save, reopen and native cleanup without changing the original Excel process. A dependency-free OSAKit prototype works with current session JSON. Its production lifecycle/transport requirements are still open and the existing-sheet guard remains unchanged. The [defined Word style read](macos-word-quality/defined-style-read/README.md) passes two exact repeated snapshots on one nonempty fixture without dirtying the document or materializing list templates; full style/page-story/quality coverage remains open.

The Windows Remote Control task is visible again. A continuation for `6653e14` was accepted, but the compact snapshot still shows the old turn and no current-candidate command evidence. No fresh Windows native execution is claimed. All six parity tasks remain partial; PR9 remains a release-blocked candidate.

The [isolated Excel process diagnosis](macos-excel-existing-delete-diagnosis/README.md) verifies distinct PID inventories and an in-memory cell write, but alert suppression, durable save and native close did not pass. No existing-sheet deletion was attempted and its guard remains unchanged. The owned test process was removed after exact ownership checks; original unsaved workbook markers, alerts and source bytes were preserved. Current hosted run `34403359923` succeeds in all four jobs at `55b27fa40835059ffad08a1d8d5a286cc3f4da52`; the remote Windows native snapshot still has no current candidate execution. These diagnostic results do not advance full parity or release gates.

Final Excel repair regression [full40](full-round40/README.md) passes **5,046 tests / 12 skips** in 215.26s from immutable `83c39eff217530f1b089c5583dda599c0b440499`, with all 386 source/configuration hashes unchanged and matching live. Fixture re-review closes exact deleted-sheet identity assertions and uncertain sentinel-creation cleanup; two fault-injection tests pass. Native `macos-excel-delete-selection/run-02` repeats all three write/save/reopen cases on that strengthened fixture and passes exact saved sheet-sequence/marker checks, source preservation and cleanup. The same production repair has separate real UI acceptance. This is a scoped Excel fix, not full parity or release approval.

The [Excel delete-selection repair](macos-excel-delete-selection/README.md) fixes stale logical worksheet indices after an allowed fresh-empty-sheet deletion. Independent scoped review passes; three native cases verify the subsequent write, save and reopen, with parent OOXML checks and a separate actual UI edit/Undo/Save/reopen. Source bytes and the synthetic sentinel are preserved; original workbook inventory is restored. Full39 passes **5,044 tests / 12 skips** from immutable `ed0da48783daaa47eb9389bfd722b86fd5f2d0ae`, with 385 unchanged hashes matching live. Existing-sheet deletion and full parity remain open. CI08 below applies to the parent of this repair.

Hosted CI08 at `5622a1615e6fd22676a3aa410a393503dab57f91` passes all four jobs: Linux Python 3.9/3.12 each **5,008 passed / 44 skipped**; Windows Python 3.9/3.12 each **4,977 passed / 75 skipped**. Run `34395516226` metadata and raw logs/JUnit are retained in [portable-ci/run-08](portable-ci/run-08/result-summary.json). This verifies the current character-read candidate's portable suite, not native Office/WPS acceptance.

The follow-up [Word manifest diagnosis](officejs-word-manifest-native-02/README.md) reproduced the activation failure in both the saved synthetic fixture and Word's own blank document. Developer discovery works, but no pane or runtime log appeared. The limited local preference check found no documented catalog-disable key and found connected experiences enabled; effective cloud/managed policy is not exhaustively checked. Temporary logging and the exact source manifest were removed, Word restarted, and the fixture hash remained unchanged. No certificate/trust change or Office.js operation occurred. The latest Windows remote snapshot remains idle on the old interrupted turn (cursor `2598579f-1dd6-4480-a080-218100d4aff5:3`), without current native command evidence. All six tasks remain partial and PR9 remains a release-blocked candidate.

Hosted CI07 run34355506749 at237691e1e016a3b1fc75febb5b383867f0d4a435 passes all four jobs: Linux Python3.9/3.12 each5,008passed44skipped; Windows Python3.9/3.12 each4,977passed75skipped. Raw four-job logs/JUnit and metadata are retained in portable-ci/run-07. This certifies the portable candidate sources, not installed Office/WPS native/UI acceptance.

Full37 passes **5,040 tests / 12 skips** in278.49s from snapshot00216a96813f9f0247f125fa98f21b4d890bec79 with384unchanged source/configuration hashes. The Mac Word table-position guard passes independent re-review; middle insertion itself remains incomplete. An offline Office.js probe passes21Node+6Python tests and independent re-review, with exact permissions/setup/removal documented in officejs-word-probe-prepared. The user subsequently approved probe enablement. The reviewed local HTTPS server and concrete short-lived certificate are prepared in officejs-word-probe-server and officejs-word-probe-bootstrap-01. The user explicitly approved temporary SSL/localhost certificate trust. The controlled native attempt is now recorded in [officejs-word-probe-native-01](officejs-word-probe-native-01/README.md): Apple scoped trust verification passed, but Word did not open the probe task pane and a separate browser rejected the certificate. No Office.js result was obtained. The exact trust rule, certificate, private key and source manifest were removed and the server stopped; cached Office registration removal remains unverified. No parity gate advanced.

The character-format getter now resolves font/shading records once per UTF-16 coordinate. Independent review and the fresh record-first native fixture validate all 220 coordinates, explicit formatting variants and saved/body/measured-count preservation. Actual production character reads take 4.415s and 4.191s. The original color-expectation failure is retained with a separate strict 16-bit RGB reassessment in [character-record-read](macos-word-quality/character-record-read/README.md). Full38 passes **5,040 tests / 12 skips** in 259.68s from immutable54ce7e624e6fbc51f76c6e83f8e385b5b6af4dc2, with384unchanged hashes equal to live. Targeted quality140PASS and staged Windows checkout paths30PASS. This does not close the full snapshot or parity gates.

Earlier checkpoints follow.

Latest local round36 passes **5,001 tests / 12 skips** in 478.47 seconds: snapshot `054ccf4662e30a6a1824e1904dde1429e0146952`, tree `4f1255cc72d2b9d48391bd8ef100d189867ccf50`, all383source/configuration hashes unchanged and equal at completion. It includes the independently reviewed early400 bridge repair and Windows budget rounding correction. Hosted run05 at `370c8c5` passes Linux3.9/3.12 (4,948 / 44 each) but fails Windows3.9 (4,916 / 1 failure / 75 skips) and Windows3.12 (4,915 / 2 failures / 75 skips). Raw request evidence contains remaining_seconds=600.0000000000002; a positive-budget clamp repairs this rounding error without extending the deadline or weakening the worker limit. Hosted run06 (`34352720870`) at `0b4f86e35baf09e58c3b817cde4e41a8b991f597` passes all four jobs: Linux Python 3.9/3.12 each 4,969 passed / 44 skipped; Windows Python 3.9/3.12 each 4,938 passed / 75 skipped. Raw logs, JUnit and metadata are retained in `portable-ci/run-06/`. These portable results do not establish installed Office native/UI acceptance.

Semantic table matrix04 obtains a valid explicit-range conversion at12..23, but creates1×3 instead of4×3. Matrix05 grid prefill then conversion returns-1708; root observes the grid still in body and only the old table. Both failure/success diagnostics retain their original results, and owned/sentinel cleanup is separately verified. No robust middle-table constructor is accepted. An interim preflight guard for unsafe public nonterminal table insertion now passes independent re-review (320 tests); full37 now passes 5,040 tests / 12 skips with all 384 source/configuration hashes unchanged. The guard checks normalized patches plus ops before opening or writing. Full position parity remains required. Existing unresolved Word quality snapshot, nine absent direct methods, Windows native/UI and Office.js/WPS add-in decisions still prevent full completion.

Earlier source-bound checkpoints follow; their counts and live-source claims apply only at their recorded execution time.

Hosted run04 at `595268e` completes with **4,811 passed / 44 skipped** on each Linux job and **4,780 passed / 75 skipped** on Windows Python 3.9. Windows Python 3.12 records **4,779 passed / 1 failed / 75 skipped**: unauthorized-origin rejection ends with WinError10053 instead of the asserted HTTP403. Raw four-job evidence is retained in `portable-ci/run-04/`; its cause remains under investigation. The prior 10 Windows failures are no longer present. Subsequent Windows Word and Excel/PowerPoint one-shot diagnostic/cleanup repairs pass scoped independent review (49 and 57 tests). Bridge rejection teardown passes 488 module tests and 31 independent targeted checks. Full round33 passed 4,968 tests / 12 skips; its integration review found one remaining proxy diagnostic-stat cancellation defect. The narrow repair now passes independent re-review (24 targeted checks). Full round34 passes **4,980 tests / 12 skips** in 394.26 seconds from immutable commit `bd8067a22741958d91a9823cdee0a1616fad62aa`, tree `aa1f49e54aa9ea81266087c0f16ae64bfa9a259b`. All 383 recorded source/configuration files are unchanged before/after and equal to the live candidate. These changes are outside run04.

Hosted run03 at `1686155` passes both Linux jobs (4,758 / 44 skips each) and leaves 10 failures on each Windows version (4,717 / 75 skips each). The follow-up Windows lock and path fixes, diagnostic persistence repair, and two cleanup exception-masking fixes now pass independent scoped review (173 tests); full round32 passes **4,843 tests / 12 skips** in 217.11 seconds from immutable commit `415c9d04f38e5d4a744baadce75a39bf76dc8cca`, tree `d47e4cc6aba2c6ff029653799f35aba51ce860b4`. All 377 recorded source/configuration hashes are unchanged before/after and match the frozen live sources. Raw JUnit, logs and hashes are in `full-round32/`. The actual hosted run04 result is recorded above.

Quality run02 passed the corrected position guard but failed header/footer count parsing before insertion. Follow-up native probes verify grouped counts and ordinal table-row reads. They also isolate two unwanted read effects: reading all styles' auto-update property materializes list templates, and requesting the text object of an absent header marks the document dirty. The defined-style and story-range alternatives remain diagnostic candidates; complete read-only snapshot and quality native acceptance are still open. Failed probes and exact-copy cleanup evidence are retained separately. The reviewed semantic-table command primitive is internal only, without public forwards or native acceptance. Native run01 failed table construction. Constructor matrix01 confirms inactive-window creation errors; root separately reconciled the active case after its diagnostic ACK failed: the 4×3 table was appended at 62..78, leaving the requested 12..19 REPLACE text unchanged. Exact-copy UI discard, sentinel verification and guarded recovery end with an empty document inventory. Activation alone does not satisfy middle-selection replacement. Matrix02 confirms that resetting selection after activation still appends at 62..78, while changing only `make` location to the range inserts at 20..36 after the selected paragraph; neither removes REPLACE. Both diagnostic copies and the sentinel close with empty inventory. The public structural table insertion path shares the original builder and lacks a location postcondition; its nonterminal-position correctness is an open bug requiring repair, not an accepted limitation.

The latest compact Windows Remote Control refresh returns cursor `a7d4eff5-c30b-4eb0-a405-6aa5c2a9fd1a:34`, idle, with the same old interrupted turn. The newly accepted dispatch has not produced current candidate command output, so no new Windows native execution is claimed.

The current working candidate has reviewed Word numbering and diagnostic-I/O repairs, a reviewed locked qs dependency update, and follow-up Windows portability fixes. Full round 30 retained **4,608 passed / 1 failed / 12 skipped**; its obsolete checkpoint-construction test has been repaired. Full round 31 passes **4,790 tests / 12 skips** in 281.77 seconds from immutable commit `4a518a70b30053399d72090d7a1ffd4c861e58a8`, tree `c7fdfbdac419faeb8baef79be7a2e7e4af1ba29c`. All 373 recorded source/configuration files are unchanged before/after and still match the live candidate. Its raw logs, JUnit and hashes are retained in `full-round31/`. Hosted run 02 passed both Linux jobs (**4,435 passed / 34 skipped** each) but failed Windows (68 failures on Python 3.12; 72 on Python 3.9). Those failures have scoped reviewed fixes and still require hosted rerun.

Current MacWordSession declares **71 of 80** direct method names; **9 remain absent**. The four new anchored-quality methods have not passed native acceptance: run-01 failed its first upsert in a read-only table-position check after successful bookmark reservation. A fresh read-only probe isolated the table collection-reference error; its parent cleanup verified unchanged text and an empty native document inventory. All original failure evidence is retained. Numbering native/UI acceptance is source-bound to its recorded earlier session implementation. These results do not close full argument, lifecycle or platform gates.

The paragraphs below retain earlier source-bound checkpoints. They are not final verification of the current working candidate.

Portable CI repair round29 passes **4,457 tests / 12 skips** in 210.72 seconds from an independent Git snapshot, with all 335 recorded Python/configuration files unchanged. Four byte-preserving historical image renames repair the Windows checkout failure; four test-file changes repair the ten Linux host-assumption failures without changing production platform behavior. Both repairs pass independent scoped review. Initial hosted failures, exact snapshot details and raw evidence are in `portable-ci/README.md`. A new hosted run is still required; concurrent Word work and the subsequent dependency repair are outside this regression snapshot.

Fresh-development-environment round28 passes **4,426 tests / 12 skips** in 239.86 seconds with all 342 recorded files unchanged, using a new Python 3.9 venv and only documented dev dependencies. The setup repair declares PyMuPDF, upgrades pip before editable installation, documents npm/Git prerequisites, and updates the dependency contract test to parse TOML. `development-setup-audit.md` retains the initial installation and round27 assertion failures separately. This evidence binds the four-file setup overlay over `53ad017`; later heading, quality and numbering work is outside that snapshot.

Delivery audit found that repository-wide Office/PDF/PNG ignores had excluded native artifacts from the checkpoint commit, including the retained PDF/DOCX pair required by an unconditional regression. Commit `53ad017` adds the 286 referenced native artifacts (5,141,284 bytes). A Git-exported copy of its exact tree `3b58f66dc5651cd4a09350ead3e4561ef55bbffd` passes **4,426 tests / 12 skips** in 202.30 seconds after preparing locked npm templates and the frozen Git baseline; all 340 recorded source hashes remain unchanged (`unit-round26*`). The first archive-only run (`unit-round25*`) retains 52 environment failures: 45 missing npm-template dependencies and seven missing Git-history checks.

Earlier frozen regression: **4,426 passed / 12 skipped** in 220.53 seconds (`unit-round24.txt`), with all 340 source/test/fixture/configuration hashes unchanged. Independent continuation re-review closes the three original P2 findings: destructive rollback ordinary-error quarantine, deterministic PowerPoint deadline tests, and detection of Word's filled PDF rule. It found no remaining worthwhile P1/P2 within that scoped repair. On the repaired source, recovery run-06 passes 13 native checks and notice run-04 passes 57 checks across six scenarios.

`macos-word-rollback-failure/run-02` passes 14 real injected-failure checks and independent scoped review: the native field is deleted, the table remains, the prefix is unchanged, and an ordinary -2700 causes quarantine. Later write/context-close attempts emit no additional scripts (7→9→9); the unrelated unsaved sentinel remains unchanged. Root then explicitly discards the exact test target in UI, independently verifies/cleans the sentinel and runs guarded recovery, ending with an empty inventory. This is manual cleanup following successful isolation, not automatic rollback recovery. Run-01 remains a separate pre-injection fixture failure caused by assuming a file-owned window ID could not be null.

`installed-audit-11` verifies all 201 copied source files, 6,426 npm dependencies and one generated runtime file before/after native public Word, Excel and PowerPoint runs. Each app passes six create/edit/save/PDF/reopen/source-preservation checks; the live source remains equal and Word inventory is empty. Personal installations remain unchanged.

Round 22's **2 failures / 4,352 passes / 12 skips** remain retained. Its fixed absolute deadline of 123 expired during the longer process; the replacement tests use a controlled clock and prove the unchanged request deadline through validation, snapshots, and publication. Paragraph-rule run-02 has 11 native/XML/reopen/cleanup checks passing and one original PDF-checker failure. `run-02-offline-correction-02` separately passes all five artifact and both retained ACK checks; all 63 original files are unchanged. No third native run is claimed.

Inline/block notices pass native run-03: six scenarios and 57 checks, including real partial-table rollback before one fallback. Pagination run-01 passes 11 native checks with three-page geometry and unchanged read-only reopen. Word notices and paragraph-rule UI evidence records edits to existing content, Undo, explicit Save, close, exact-file reopen and close, preserving the original file bytes and relevant XML. These reports bind their recorded source snapshots. At that earlier checkpoint MacWordSession declared 66 of 80 direct method names; 14 were absent, and argument/lifecycle/platform requirements remain separate.

The six approved parity tasks remain partial; the explicit remaining implementation, native, UI, Windows and certification checklist is in `task-completeness-resume-audit.md`. The native text/table/field checkpoint implementation has passed independent scoped review and final `macos-word-recovery/run-05` passes **13 native checks**. These include actual controller partial failure → acknowledged rollback → fallback/issue once, old REF/TOC handles, saved DOCX/PDF, reopen, unchanged unsaved sentinel across owned close, unchanged image/textbox no-op, and rejection of newly added textbox before deletion. The parent verified all nine retained/current source hashes and both PDF pages. Shape/figure/equation deletion rollback remains unsupported.

Run 04 remains a retained failure: after sentinel close, Word's native document collection became unavailable while the exact private target was still visible in UI. The quarantined task-owned document was explicitly discarded via its exact Word window, then native inventory and the guarded recovery routine confirmed cleanup. No automatic recovery success is inferred. A reviewed fixture-only change closes the owned target before independently closing its exact verified sentinel.

`ui-word-recovery-01` records actual existing-text replacement → Undo → explicit Save → close → same-path UI reopen → close. The saved copy and original source retain identical SHA-256, text and field codes, and final native inventory is empty. The subsequent notices and pagination results are recorded above; paragraph-rule acceptance remains in progress.

Full round 20 passed **4,177 tests / 12 skipped** in 198.53 seconds with 327 source/test/fixture/configuration hashes unchanged across the run. Later installer and fixture edits require a subsequent regression; this is not final parity clearance.

The installer audit found `.superpowers` scratch files were being copied. Exact `.superpowers` / `.worktrees` exclusions now pass independent review and 11 installer tests. Fresh `installed-audit-09` compares **all 193 copied source files**, separately inventories **6,426 npm dependency files** and the generated runtime JSON, and confirms neither scratch directory is installed. Word, Excel and PowerPoint each pass six installed public native checks. All installed bytes remain equal to their captured snapshot; the recovery fixture later changed and the new degradation module was not in that snapshot, so this is not final-current-source installation acceptance. Earlier 118-file comparisons covered selected skill/runtime/manifest files, not every installed file.

Windows Remote Control accepts dispatch and reports state changes, but its latest returned turn and command output are still the old interrupted run. No new candidate execution is claimed. Existing release/merge and personal installation boundaries remain in force.

| Area | macOS evidence available | Windows evidence for this candidate |
|---|---|---|
| Word advanced generation | `macos-word-advanced/final-normal-02` and `final-source-fallback-02`: native objects, fields/numbering, PDF/reopen, controlled fallback, sentinel | Pending new candidate execution |
| Excel generation/conversion | `macos-excel/public-run-01` and `public-run-02`: public route, formulas/caches, PDF, unchanged input | COM-free host/runtime tests only |
| PowerPoint generation/conversion | `macos-powerpoint/public-native-02`: public route, native shapes/images/tables, PDF/reopen | COM-free host/runtime tests only |
| Word file edits | `macos-word-sessions/native-10` and `new-public-05-reviewed`: public new document, formatting/structure, native save/PDF/reopen | Session/proxy tests; native execution pending |
| Excel business/session APIs | `macos-excel-sessions/business-04`, `structural-04`, `active-03`, `new-03`: public factory, formulas/charts/conditions, copy/move boundaries, saved active binding | Session/proxy tests; native execution pending |
| PowerPoint business/session APIs | `macos-powerpoint-sessions/semantic-03-public`, `save-current-01`: all current semantic method signatures, native save/current/reopen | Session/proxy tests; native execution pending |
| UI editability | `ui-word-01`, `ui-excel-01`, `ui-powerpoint-01`: actual edits to existing content, Undo, explicit save, close and UI reopen; Excel formula/cache and saved PPT texts/shape counts preserved | Pending |

The current Mac implementation includes reviewed Word typography, objects and section methods, shared request-wide Mac Microsoft edit preflight, and guarded initial PowerPoint sizing. The fields/index slice has passed scoped follow-up review. Native evidence and limitations are detailed below. `fixtures/microsoft_parity/windows_native.py` prepares bounded three-app Windows acceptance; portable tests and older Windows evidence do not establish execution of this candidate. Independent reviews are scoped implementation checks, not final whole-parity approval.

The follow-up adds 13 more direct Mac Word methods for paragraph styles, headings, code paragraphs, columns, sections, footer PAGE fields and field refresh. The 55 new tests include syntax-only compilation; four independent-review findings were fixed (style-name substitution, inheritance cycles/case aliases, incompatible style types, and a two-column fixture that reset itself before saving). Native execution resumed on September 9; it exposed a Consolas East Asian font-slot rejection and a footer PAGE field being inserted into the main story. Both defects are fixed: final `macos-word-business/resume-03` and `macos-word-business-followup/resume-03` pass 9 and 10 native/artifact checks respectively. Independent review confirmed font slots, exact footer fields, source hashes and three-section header inheritance; the false-positive field validator was then strengthened with 21 pure tests and re-reviewed. After reviewed object, section, field/index and reference/bibliography slices, 59 of the frozen 80 direct Word method names are declared; 21 [remaining methods](word-direct-remaining.md) are still absent. Name presence is not a support or native-coverage percentage.

Full suite round 8 passed **3,717 tests with 12 skips** in 186.73 seconds; see `unit-round8.txt`. The skips include opt-in native gates and are not native passes. After all scoped production fixes, round 9 passed **3,731 tests with 12 skips** in 244.25 seconds; see `unit-round9.txt`.

`fixtures/microsoft_parity/macos_powerpoint_size_probe.py` is a guarded hypothesis test for width/orientation swaps. The first native run confirms 720×405 and 405×720 saved/reopened/PDF dimensions, but resizing moved existing shapes (the landscape PDF became blank). `run-02` verifies size-before-content dimensions, exact textbox/rectangle geometry, nonempty PDF and native reopen for both orientations. Production initial-sizing integration and independent review passed. `macos-powerpoint-initial-size/run-05` verifies public creation at 720×405 and 405×720, precise geometry/PDF/native reopen, unchanged source files, rejected existing-content resizing without changes, lower-level recorded-plan generation at 720×405, and default public generation at 960×540. Its fixture/compiler/session/runtime hashes match the reviewed source. Public `generate()` gains no size argument. Existing-content resizing and untested dimension combinations remain open. Unrelated document text is hashed in memory before logging, including timeout diagnostics.

Representative evidence does not validate every argument combination, every baseline row, the exact final branch, or installed-plugin behavior. Older failed runs and corrected reruns are retained separately. Word `new-public-02` passed its original narrow assertions but visual review exposed concatenated paragraphs; `new-public-03-boundary-red` proves the defect, and `new-public-04-boundary`/`new-public-05-reviewed` verify the strengthened paragraph assertion.

Outstanding work:

- Close all remaining required semantic/format rows: Word direct business methods, advanced styles/list glyphs/resources, full patch dimensions and structural targets, legacy formats and native SaveAs targets, and arbitrary PowerPoint size.
- Verify every argument and behavior of the frozen seven spreadsheet and nine presentation plan operations. Their operation IDs are implemented; limitations such as arbitrary slide height remain open. The richer direct Composer methods are separate required rows; adding new plan operation IDs beyond the frozen grammar is not necessary for parity.
- Native-dependent COM value acceptance and target lookup remain runtime checks. Windows `auto` now preflights proven target/property and conversion constraints before opening either engine; candidate-specific structural checks preserve WPS preference and select Microsoft for explicit worksheet clone-to-start/end. Independent review passed 155 focused and 173 routing tests. Unknown COM values and unclassified structural properties stay deferred, so this is not complete request-capability certification. Mac preflight shares executors' validators and preserves nested table generators.
- Resolve macOS native gaps: trustworthy unsaved/active binding where native IDs are absent, non-rebinding attached save-copy/PDF, and existing Excel worksheet deletion without a confirmation dialog. The proposed local Office.js supplement still requires the user's design choice; it has not been installed.
- Finish independent reviews, native fault/recovery tests and source-bound per-row evidence. Windows persistent child deadlines and typed semantic handles have platform-independent coverage only.
- Execute Windows native business acceptance. The 32 missing Word methods and two PowerPoint methods found at `428283e` now pass the public transport contract; explicit rich-text, field, date, tuple and handle serialization is implemented. Independent review closed unknown-DTO-field and tuple-preservation findings; 143 portable tests pass. Actual Windows native objects and desktop results remain unverified.
- Run Windows native acceptance on the review candidate and Windows UI edit/Undo/save/reopen. Mac all-app UI, WPS M5 native regression, and isolated three-app public smoke checks have representative passing evidence; final source-bound per-row coverage and whole-parity review remain open. On September 9 the remote project reappeared and the task became readable. New continue requests were accepted but still returned the old interrupted Word prototype turn; fresh command execution must be restored for dependent native gates.
- Update the existing draft PR #9 after the new native fixes and checks stabilize. The last verified pushed candidate is this runtime repair checkpoint (parent `595268e`); no new merge, tag, release or personal installed-skill update has been performed for this parity work.

Only the previous v0.9.0 release has release acceptance. This candidate must not be described as fully matching WPS until the outstanding gates are closed.

`fixtures/microsoft_parity/evidence_gate.py` now expands the unchanged 628 capability rows into 1,256 platform gates. `coverage-checkpoint-03.json` intentionally has no certified claims: existing representative evidence has not been retroactively bound to the evolving source tree. This is an evidence-certification count, not a count of implemented features or passing native examples. A closing claim must identify an implemented capability, native verification, exact source digest (including pinned runtime dependencies), report hash, platform/engine/component, and an explicit capability-to-check mapping with passing named checks. Source changes reopen certification. Independent runner review, UI acceptance, and installation/release gates remain separate.

September 9 regression and installation: the current WPS M5 native gate passes six fixture PDFs (4/4/5/3/2/1 pages) plus 63 performance pages on WPS 12.1.28492, with only the two expected formula degradation codes. Seven hashes and representative visuals were checked; `wps-native-20260909/summary.json` points to full local evidence. The fresh isolated installation `installed-resume-02` contains 115 source-equal shipped files; installed Word, Excel and PowerPoint public create/edit/save/PDF/reopen each pass six checks. Hash equality was rechecked after all three runs. This is a private test installation; no personal plugin was updated. Later production edits reopen source equivalence.

September 9 object-method checkpoint: `add_table`, `add_merged_table`, `add_image`, `add_image_block`, and `add_floating_textbox` now have exact public signatures, shared raster normalization, deadline-aware private staging, conservative structural-ID invalidation and native acknowledgement checks. Independent review closed six findings, including default content-aware table width and image final-size validation. `macos-word-business-objects/run-05` passes all **21** named checks on Word 16.112.3, with source hashes, save/reopen/PDF, exact geometry and nonvacuous synthetic unsaved-sentinel preservation/close. Earlier FAIL/PARTIAL reports remain unchanged. Floating image alt text, unverified wraps and floating image blocks remain explicitly unsupported; method presence is not full parameter coverage. Word's unusable table-level cell count was rejected by a minimal native probe; exact merged-cell topology is verified from the saved native DOCX.

The next isolated installation `installed-resume-03` has 115 shipped files still hash-equal to the reviewed production source after the final three-app smoke. Installed Word, Excel and PowerPoint each pass six public create/edit/save/PDF/reopen/source-preservation checks. The Word probe and business-output artifact verifiers were strengthened after three independent P2 findings: exact vector dimensions, drawing relationship resolution and source-image integrity, exact merge topology, and actual PDF image placements. Original reports remain immutable; corrected offline revalidation is recorded separately. Full suite round 11 retained the three in-flight verifier RED failures; it is not a passing final gate.

Final object/preflight checkpoint validation: full round 12 passes **3,826 tests / 12 skips** in 343.27 seconds after the production freeze and checker fixes. A final exact-merge negative test was added afterward; the four-file scoped supplement passes **225 tests** in 5.50 seconds. The pure/native/UI/installation results remain distinct, and Windows native execution is still unconfirmed.

September 9 comprehensive review follow-up: full round 14 passes **3,985 tests / 12 skips** in 187.69 seconds before later PowerPoint fixes. Public source-publication conflict checks, Excel logical SaveAs/current-save, clone-to-end, startup validation and UTF-16 degradation ranges have portable regressions. Independent review then closed three additional publication/lock findings; the parent rerun passed 107 tests and independently verified `macos-excel-sessions/review-fixwave-03` artifact hashes, source preservation and save/copy/current binding. Windows/public/transport integration subsequently passed 328 focused tests.

Word fields `run-10` passes representative native 13-page output, two three-page TOCs, filtered caption indexes, REF/STYLEREF/SEQ/PAGE/NUMPAGES, exact stale-bookmark rejection and source-preserving save/reopen. `ui-word-fields-01` verifies real character editing, Undo, explicit save and UI close/reopen with preserved saved text/field codes. Independent review identified a remaining snapshot-offset bug when a tracked index grows and two verifier gaps; the slice remains open until those repairs and affected native checks pass. PowerPoint logical SaveAs/current-save and foreground-independent slide creation now pass `macos-powerpoint-logical-save/run-04` (10 native checks) and independent scoped review (191 tests). The parent independently checked three current source hashes, seven output hashes and five reopened documents' exact saved text. Failed runs and exact owned-document recovery remain retained. Attached save-copy, PDF and every view/layout combination are outside that fixture's proof.

Word field review repairs pass 153 focused tests and `macos-word-fields/run-12` (11 native checks) on the final helper, including actual TOC growth before a following unchanged REF, stable semantic keys, exact field counts, four separate retained source files, and source-preserving reopen. The parent checked all four source/copy hashes, four output hashes and the actual changed TOC/stable REF results, and viewed the growth and filtered-index PDFs. Independent scoped re-review closes all three original P2 findings, with 153 tests and no new P1/P2 in the repair diff. `run-11` retains the larger 13-page layout evidence; its earlier helper differs by the subsequent diagnostic-retention flag and is not represented as final-source evidence.

September 9 audit round 15: **4,037 passed / 12 skipped** in 192.58 seconds (`unit-round15.txt`). This run includes Word logical-save, field/index review repairs, PowerPoint logical-save/background append, Excel output-keyword compatibility, WPS inspection isolation and Windows DTO changes. The six passive M2 bridge skips and six opt-in native gates are not native passes.

`installed-audit-04` is a fresh private installation of 117 shipped files. Word, Excel and PowerPoint each pass six public native create/edit/save/reopen/PDF/source-preservation checks. Every installed file and live source hash was rechecked after all three apps completed. Personal installations remain untouched. Word logical-save `macos-word-logical-save/run-01` passes ten native cases, including SaveAs/copy/current, PDF binding preservation, two concurrency windows, exact reopens and nonempty unsaved sentinel preservation; scoped review is in progress.

WPS inspection regression exposed a reused process that opened the startup document but never loaded the temporary presentation add-in. A paired isolated-host probe registered in 2.757 seconds. Inspection/editing now host only the selected component and use the existing owned isolated launch mechanism. `wps-inspection-edit-20260909` verifies public inspect → text edit → save → reinspect → five-page PDF, unchanged source and preservation of the pre-existing WPS process after test-owned hosts exit. Raw original failure and diagnostic runs remain retained. This is bounded WPS regression evidence; final integration review and full Microsoft parity remain open.

Expanded WPS regression: `wps-inspection-other-components-20260909` records Writer inspection passing and Spreadsheet inspection failing before any profile request, despite an owned isolated ET process opening the official activation file. Both original sources and the pre-existing WPS process were preserved. ET add-in discovery/enable/trust is the probable boundary; lack of a cached ET authorization record alone is not proof of the unique cause. Enabling the local ET add-in through the official deployment entry and trusting its loopback origin changes persistent host permissions, so user authorization is pending. No authorization cache or security setting has been changed.

Full round 16 passes **4,039 / 12 skips** in 192.65 seconds (`unit-round16.txt`), including the Word fingerprint-recovery fix. Word logical-save final-source `run-02` passes ten native cases and independent 404-test review. The attempted lock-release finding was withdrawn: failed saving-close must preserve an open session and its exclusive lock until retry or discard. Integration round 2 separately identified two P2 issues in shared publication final-validation identity checks and Excel failed-saving-close ownership; their repairs and final regression remain in progress.

Integration round 2 re-review closes Excel failed-saving-close ownership with 120 focused tests and eight native checks in `macos-excel-sessions/integration-round2-01`. The parent verified retained source/output hashes and exact application inventory restoration. This native report binds transport `b211281b` and Excel session `a052c08e`; later transport repairs require new final-source evidence. Shared publication remains open: four additional RED cases cover a same-inode/size/restored-mtime external change followed by validator failure or deadline exhaustion. The five missing reference/bibliography direct methods are in implementation; checkpoint/rollback dependencies and remaining argument coverage are not waived.

September 9 round 17: **4,098 passed / 12 skips** in 211.73 seconds (`unit-round17.txt`). Shared publication and Excel close repair pass independent parent review plus 136 focused tests; final-source `macos-excel-sessions/integration-round2-02` passes all eight native checks. Word reference/bibliography `macos-word-references/run-04` passes eight native checks and independent 217-test review, after three real-host defects were fixed and their earlier failures retained. Five direct names are now callable, but the separate real controller checkpoint/rollback chain remains open. The parent independently verified final dependency/artifact hashes, REF/SEQ topology, exact fallback style boundaries and PDF visuals.

`installed-audit-05` is a fresh private installation, with 118 skill/runtime and manifest files compared. Word, Excel and PowerPoint each pass six public native create/edit/save/reopen/PDF/source-preservation checks; all live source and installed hashes still agree after all three runs. The first installer invocation rejected an incorrectly nested marketplace path before changing the destination; the corrected explicit isolated path and both logs are retained. Personal installations remain untouched. Final integration review and new-reference UI acceptance are still in progress; Windows, remaining semantic/format rows and full source-bound certification remain open.

Integration round 3 closes three related Windows saving-close findings: acknowledged save conflicts preserve session/handles, native-close uncertainty still quarantines, and the worker re-verifies the exact native binding within the original deadline before asserting an open session. Independent scoped review passes 452 focused tests and five reviewer cases; evidence is in `integration-review-round3`. Windows native execution remains unverified.

Full round 18 reports **4,106 passed / 12 skips** in 236.65 seconds (`unit-round18.txt`), but the final Windows binding-recheck repair was being added during this run, so it is an intermediate regression result, not a frozen final gate. `installed-audit-06` records a successful installation with a 118-file skill/runtime and manifest comparison before that repair; no native installed smoke is claimed for that snapshot.

Word reference UI acceptance is complete in `ui-word-references-01`: edit, Undo, explicit Save, close, exact recent-file reopen and close, with original text/field codes/source hash preserved. Checkpoint probes remain partial: text/CR cases pass, table rollback is unresolved. A cancellation during the probe exposed missing Word transport quarantine for KeyboardInterrupt/SystemExit; repair and targeted fault tests are in progress. All probe failures remain retained, and root CUA exact sentinel cleanup plus empty inventory is recorded in run-05/parent-recovery.json.

Final repair checkpoint, round 19: **4,112 passed / 12 skipped** in 239.56 seconds. Shipped source and test hashes are unchanged across this full run (`unit-round19-source-hashes.json`). The skips remain six passive bridge gates and six opt-in native gates. The Word transport interruption repair passes independent scoped review (181 tests plus four fault cases); uncertainty now quarantines before context exit can send a second AppleEvent, while preserving the original cancellation exception.

`installed-audit-07` contains a fresh private installation, with 118 skill/runtime and manifest files compared. Word, Excel and PowerPoint each pass six public native create/edit/save/PDF/reopen/source-preservation checks. After all three runs, every installed and live source hash still matches the manifest; Word's native document inventory is empty. This is representative installed Mac acceptance and does not cover the missing direct methods or Windows.
